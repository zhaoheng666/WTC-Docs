# NPM 配置文件
# 解决 Rollup 架构兼容性问题

# 启用可选依赖的安装
optional=true

# 不因为可选依赖失败而中断安装
omit=optional

# 增加网络超时时间（毫秒）
network-timeout=60000

# 使用更宽松的 SSL 验证（仅在必要时启用）
# strict-ssl=false

# 清理无用的元数据
prefer-dedupe=true

# 使用更快的包解析算法
prefer-offline=true

# 静默安装可选依赖的警告
loglevel=warn