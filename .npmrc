# 确保安装所有可选依赖
optional=true

# 不忽略脚本
ignore-scripts=false

# 使用精确版本
save-exact=false

# 确保平台特定依赖被正确安装
force=true

# 提高安装的稳定性
prefer-offline=false
fetch-retries=3
fetch-retry-mintimeout=10000
fetch-retry-maxtimeout=60000

# 对于 ARM64 Mac，确保正确的架构
# npm 会自动检测，但明确设置更安全
# arch=arm64
# platform=darwin